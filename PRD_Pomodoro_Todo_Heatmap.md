# 포모도로 타이머 + 투두 + 집중 히트맵 웹 앱 PRD

## 1. 한 줄 설명
프로모도로(25분 집중) 타이머 + 할 일 목록 + 하루별 집중 시간 히트맵을 한 화면에서 보여주는 웹 앱이다.

## 2. 배경 및 문제 정의
집중을 시작하는 도구(타이머)와 해야 할 일(투두), 그리고 내가 얼마나 집중했는지(기록/시각화)가 서로 분리되어 있어 사용 흐름이 끊기는 문제가 있다.  
본 제품은 “집중 시작 → 할 일 확인 → 누적 성과 확인”을 단일 화면에서 끝내는 것을 목표로 한다.

## 3. 목표
- 타이머/상태 관리를 통해 실시간 UI 업데이트 경험을 제공한다.
- 집중 시간 데이터를 누적하고 간단한 시각화(히트맵, 막대 그래프)를 제공한다.
- 별도 회원가입 없이 브라우저만으로 사용 가능하도록 구성한다.
- 로컬 저장(localStorage) 기반으로 1인 사용 시나리오를 완결한다.

## 4. 비목표
- 계정/로그인, 클라우드 동기화, 팀 공유 기능은 범위 밖이다.
- 복잡한 통계(생산성 예측, AI 추천)는 범위 밖이다.
- 다양한 포모도로 커스텀(긴 휴식, 라운드 관리, 태스크별 타임로그)은 후순위이다.

## 5. 타깃 사용자
- 공부/업무 집중 시간을 빠르게 재고 기록하려는 사용자이다.
- 할 일 체크리스트와 집중 기록을 함께 보고 싶은 사용자이다.

## 6. 핵심 사용 시나리오
1) 사용자가 투두를 추가한다.  
2) 사용자가 포모도로를 시작한다.  
3) 타이머 진행 상황이 원형 progress bar로 실시간 표시된다.  
4) 1회 타이머가 끝나면 오늘 날짜에 +25분이 누적된다.  
5) 누적 데이터가 즉시 히트맵과 막대 그래프에 반영된다.  
6) 사용자가 투두를 완료 체크하거나 삭제한다.  
7) 브라우저를 닫아도 localStorage로 데이터가 유지된다.

## 7. 정보 구조 및 레이아웃
- 왼쪽: 포모도로 타이머 영역이다.
- 오른쪽 위: 투두 리스트 영역이다.
- 오른쪽 아래: 7일/30일 집중 히트맵 및 요약 영역이다.

## 8. 기능 요구사항

### 8.1 포모도로 타이머
- 기본 집중 시간은 25분이다.
- 상태는 Idle(대기), Running(진행), Paused(일시정지), Finished(완료)로 구분한다.
- 시작/일시정지/리셋 버튼을 제공한다.
- 진행률은 원형 progress bar로 표현한다.
  - 구현은 SVG circle의 stroke-dashoffset을 활용한다.
- Running 상태에서는 1초 단위로 남은 시간을 감소시키고 UI를 갱신한다.
- Finished 시점에 “오늘 날짜에 +25분 누적” 규칙을 적용한다.
- Finished 이후 타이머는 Idle로 복귀하거나, 사용자가 명시적으로 리셋하도록 정책을 고정한다(최소 1가지로 단순화한다).

### 8.2 투두 리스트
- 투두 항목 추가 기능을 제공한다.
- 각 항목은 완료 체크 기능을 제공한다.
- 각 항목은 삭제 기능을 제공한다.
- 투두 데이터는 localStorage로 저장한다.
- 기본 정렬은 생성 순서(오래된 항목이 위)로 한다.
- 완료 항목은 시각적으로 구분(취소선/투명도)한다.

### 8.3 집중 시간 기록(일별)
- “날짜별 총 집중 시간(분)”을 기록한다.
- 1회 포모도로 완료 시 오늘 날짜에 +25분을 누적한다.
- 날짜 키는 사용자 로컬 시간대 기준 YYYY-MM-DD 문자열로 통일한다.

### 8.4 집중 히트맵(7일/30일)
- 7일 보기와 30일 보기를 토글로 제공한다.
- 각 날짜 셀은 해당 날짜의 누적 집중 시간(분)에 따라 색 강도를 갖는다.
- 색 강도 구간은 다음과 같이 정의한다.
  - 0~10분: 연한색이다.
  - 10~30분: 중간색이다.
  - 30분 이상: 진한색이다.
- 타이머 완료로 데이터가 갱신되면 히트맵 색상이 자동 업데이트된다.

### 8.5 간단 시각화(막대 그래프)
- 최근 N일(7 또는 30)에 대한 날짜별 집중 시간을 막대 그래프로 표시한다.
- 그래프는 히트맵과 동일 데이터 소스를 사용한다.
- 그래프의 목적은 “대략적인 추세 확인”으로 제한한다.

### 8.6 데이터 저장
- 투두와 집중 기록은 localStorage에 저장한다.
- 저장 키는 명시적으로 분리한다(예: todos, focusByDate, settings).
- 앱 로드 시 localStorage를 읽어 초기 상태를 복원한다.
- 저장 실패(스토리지 제한/프라이빗 모드 등) 시 화면에 안내 메시지를 표시한다.

## 9. 데이터 모델(초안)

### 9.1 Todos
- todos: Array<Todo> 형태이다.
- Todo 스키마는 다음과 같다.
  - id: string(유니크)이다.
  - text: string이다.
  - completed: boolean이다.
  - createdAt: number(epoch ms)이다.
  - completedAt: number | null(epoch ms)이다.

### 9.2 Focus 기록
- focusByDate: Record<string, number> 형태이다.
  - key: "YYYY-MM-DD"이다.
  - value: minutes(number)이다.

### 9.3 Settings(선택)
- settings: Record<string, any> 형태이다.
  - pomodoroMinutes: number(기본 25)이다.
  - viewRange: "7d" | "30d" 이다.

## 10. 상태 관리 요구사항(타이머)
- 단일 소스의 진실(SSOT)로 타이머 상태를 유지한다.
- UI는 상태 변화(시작/일시정지/리셋/완료)에 따라 즉시 갱신된다.
- 권장 상태값은 다음과 같다.
  - status: "idle" | "running" | "paused" | "finished" 이다.
  - startedAt: number | null 이다.
  - remainingSeconds: number 이다.
  - durationSeconds: number(= 25*60) 이다.

## 11. 엣지 케이스 및 정책
- 브라우저 새로고침/닫기 중 Running 상태 처리 정책을 정한다.
  - 옵션 A: 앱이 다시 열리면 즉시 paused로 복원한다.
  - 옵션 B: startedAt을 저장하고 경과 시간을 계산해 running을 복원한다.
  - MVP에서는 옵션 A를 우선한다(오류 가능성 감소 목적)이다.
- 날짜 경계(자정) 근처에서 완료될 때 누적 날짜 기준은 “완료 시점의 로컬 날짜”로 한다.
- 여러 탭 동시 사용은 비권장으로 두고, 마지막 저장이 덮어쓰는 정책을 기본으로 한다.

## 12. 접근성/반응형
- 키보드만으로 투두 입력/추가가 가능해야 한다.
- 모바일에서도 1스크린에 핵심 요소가 보이도록 반응형 레이아웃을 제공한다(세로 배치로 전환)이다.
- 색상만으로 정보를 전달하지 않도록, 셀 hover/클릭 시 분(min) 텍스트를 표시한다.

## 13. 성능 요구사항
- 30일 데이터 렌더링은 즉시 수행된다(체감 지연 없음)이다.
- setInterval 기반 타이머는 탭 비활성화에서 지연될 수 있으므로, 남은 시간 계산은 startedAt 기반으로 보정 가능하도록 설계한다(옵션)이다.

## 14. 성공 지표(정성/정량)
- 정성: 사용자가 “한 화면에서 흐름이 끊기지 않는다”는 피드백을 얻는다.
- 정량: 재방문율(로컬 기준), 1일 평균 완료 세션 수, 투두 완료 체크 수를 이벤트로 측정 가능하다(클라우드 분석 도입 시)이다.

## 15. 수용 기준(핵심)
- 타이머 시작/일시정지/리셋이 정상 동작한다.
- 원형 progress bar가 시간 경과에 따라 자연스럽게 갱신된다.
- 1회 완료 시 오늘 날짜 집중 시간이 정확히 +25분 누적된다.
- 히트맵이 7일/30일 토글에 맞춰 렌더링된다.
- 히트맵 색상 구간(0~10, 10~30, 30+)이 정상 적용된다.
- 투두 추가/완료/삭제가 동작하고 localStorage로 유지된다.
- 새로고침 후에도 투두/집중 기록이 복원된다.

## 16. 오픈 이슈(결정 필요)
- 타이머 완료 알림(소리/브라우저 알림) 제공 여부를 결정한다.
- 휴식 타이머를 MVP에 포함할지 결정한다.
- “오늘 +25분 누적” 규칙을 고정할지, 실제 집중 시간(중단/재개 반영)으로 계산할지 결정한다.
